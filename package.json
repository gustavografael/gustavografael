{
  "name": "checkpoint-firewall-troubleshooting-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Dashboard moderno para troubleshooting de firewalls Check Point via SSH.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
